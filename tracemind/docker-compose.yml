services:
  tracemind-server:
    build: ./server
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - DATABASE_URL=sqlite:///./data/tracemind.db
    restart: always
